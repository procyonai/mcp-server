# Microsoft Azure AD Configuration
# Replace with your Azure AD tenant details

PORT=8080
SERVER_URL=http://localhost:8080

# Azure AD-specific configuration
OIDC_CLIENT_ID=your-azure-application-client-id
OIDC_CLIENT_SECRET=your-azure-client-secret
OIDC_AUTH_URL=https://login.microsoftonline.com/your-tenant-id/oauth2/v2.0/authorize
OIDC_TOKEN_URL=https://login.microsoftonline.com/your-tenant-id/oauth2/v2.0/token
OIDC_USER_URL=https://graph.microsoft.com/oidc/userinfo
OIDC_SCOPES=openid profile email

# Instructions:
# 1. Copy this file to .env in the project root
# 2. Replace "your-tenant-id" with your Azure AD tenant ID
# 3. Replace OIDC_CLIENT_ID and OIDC_CLIENT_SECRET with your Azure application credentials
# 4. Start the server: ./mcp-server
#
# Azure AD Setup:
# 1. Go to Azure Portal (https://portal.azure.com/)
# 2. Navigate to Azure Active Directory -> App registrations
# 3. Click "New registration"
# 4. Set Name and select "Accounts in this organizational directory only"
# 5. Add Redirect URI (Web): http://localhost:8080/callback
# 6. After creation, go to "Certificates & secrets" -> New client secret
# 7. Copy the Application (client) ID and the client secret value
# 8. Note your Tenant ID from the Overview page