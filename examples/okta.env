# Okta Configuration
# Replace with your Okta organization details

PORT=8080
SERVER_URL=http://localhost:8080

# Okta-specific configuration
OIDC_CLIENT_ID=your-okta-client-id
OIDC_CLIENT_SECRET=your-okta-client-secret
OIDC_AUTH_URL=https://your-domain.okta.com/oauth2/default/v1/authorize
OIDC_TOKEN_URL=https://your-domain.okta.com/oauth2/default/v1/token
OIDC_USER_URL=https://your-domain.okta.com/oauth2/default/v1/userinfo
OIDC_SCOPES=openid profile email

# Instructions:
# 1. Copy this file to .env in the project root
# 2. Replace "your-domain" with your Okta domain (e.g., mycompany.okta.com)
# 3. Replace OIDC_CLIENT_ID and OIDC_CLIENT_SECRET with your Okta application credentials
# 4. Start the server: ./mcp-server
#
# Okta Setup:
# 1. Go to your Okta Admin Dashboard
# 2. Applications -> Create App Integration
# 3. Choose "OIDC - OpenID Connect" and "Web Application"
# 4. Set Redirect URI to: http://localhost:8080/callback
# 5. Copy the Client ID and Client Secret